apiVersion: v1
kind: Service
metadata:
  name: reviewsapi-service
  namespace: fcg-reviews
spec:
  selector:
    app: reviewsapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
